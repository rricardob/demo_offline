spring:
  application:
    name: "bloggio-api"
  datasource:
    #url: ${URL_DB}
    url: jdbc:postgresql://dpg-cuva6hi3esus73bna9f0-a.oregon-postgres.render.com/ferreteria_p9rp
    #username: ${USER_DB}
    username: ferreteria_p9rp_user
    #password: ${PASSWORD_DB}
    password: 5gzRGBZUIUIw9fmpmHFkPLdfA5TrkIGp
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      javax:
        persistence:
          validation:
            mode: none
    show-sql: true
    hibernate:
      ddl-auto: update
#    open-in-view: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher